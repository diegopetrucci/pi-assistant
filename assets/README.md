This directory houses shared fixtures (prompt snippets, reference JSON blobs, WAV samples)
that aren't code or configuration. Keep large binary inputs out of `src/` and `tests/`
and drop them here with a short description of how they were produced.
